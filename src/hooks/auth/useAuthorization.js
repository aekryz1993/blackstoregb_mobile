import {useState} from 'react';

export default () => {
  const [auth, setAuth] = useState();

  // useEffect(async () => , [data])

  return {
    // isAdmin,
    // isActive,
    // isAuth,
    // permission,
  };
};
